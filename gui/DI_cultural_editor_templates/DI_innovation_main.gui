template DI_Innovation_Main
{
    vbox = {
        layoutpolicy_horizontal = expanding
        datacontext = "[GetPlayer.MakeScope.Var('DI_innovation_selected_culture').Culture]"

        using = DI_all_innovation_groups_pod
    }
}